package baseline.functions;

public class Error {
    /* Errors only have an "ok" button. Depending on what proc'ed the error, it will have a string
    that this will print out. and then Users can only exit out.
     */
    public void printError(String Input){
        //take whatever string  we got from our classes
        //and set is as a label and open up that error screen
    }
}
